# Core ML stack
torch>=2.0.0
torchvision
lightning
jaxtyping

# Hiera backbone (official facebookresearch implementation)
hiera-transformer
timm

# Configuration and Logging
hydra-core>=1.3.0
omegaconf
comet-ml

# KANGA / SSM dependencies (Unix only)
causal-conv1d>=1.4.0
mamba-ssm

# Dataset utilities
huggingface_hub
av

# Augmentation (TPS + affine for VOS training)
kornia>=0.7.0

# Validation
pytest
