# Core ML stack
torch>=2.0.0
torchvision
lightning
jaxtyping

# Configuration and Logging
hydra-core>=1.3.0
omegaconf
comet-ml

# KANGA / SSM dependencies (Unix only)
causal-conv1d; sys_platform != "win32"
mamba-ssm; sys_platform != "win32"

# Validation
pytest
